name: water-potability-mlproject
conda_env: conda.yaml
entry_points:
  main:
    parameters:
      data_path:  # GANTI dari data_file ke data_path
        type: str
        default: "water_potability_preprocessing.csv"
      experiment_name:
        type: str
        default: "water-potability-ci"
      run_name:
        type: str
        default: "automated-run"
    command: |
      python modelling.py \
        --data_path {data_path} \
        --experiment_name {experiment_name} \
        --run_name {run_name}
